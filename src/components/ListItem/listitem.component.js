import React from 'react';

export const Storylistitem = (stories) => {
  return (
    <>
      <ul>
        {/* {stories.map((x) => (
          <li key={stories.id}>{x.by}</li>
        ))} */}
      </ul>
    </>
  );
};
